node_modules/
dist/
.wrangler/
wrangler.production.jsonc
worker-configuration.d.ts
bin/
*.log
.DS_Store

# Local credentials and deployment overrides. Keep the documented examples.
.dev.vars
.dev.vars.*
.env
.env.*
!.env.example
# Build-time values that Vite compiles into a public bundle: an origin and an
# empty string, no credentials. Ignoring it is what let development values
# reach production twice, since there was then nothing to override .env.local.
!app/.env.production
*.pem
*.key

# Local editor and test artifacts.
.idea/
.vscode/
coverage/

wrangler.local.jsonc
.wrangler
.gstack/
