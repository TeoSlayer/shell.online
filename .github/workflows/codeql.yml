# DISABLED. Every line below is commented out so that nothing in this file
# runs. Turn it back on by removing one leading "# " from each line:
#
#   sed -i '' 's/^# \{0,1\}//' .github/workflows/codeql.yml
#
# Disabled deliberately to get 0.9.0 in front of real traffic without waiting
# on the full matrix. The checks themselves still pass; they were green on the
# commit this was done from. Nothing here is broken, it is switched off, and
# it should go back on once production has been exercised end to end.

# name: CodeQL
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#   schedule:
#     - cron: "17 4 * * 1"
#
# permissions:
#   contents: read
#   security-events: write
#
# jobs:
#   analyze:
#     runs-on: ubuntu-latest
#     timeout-minutes: 20
#     strategy:
#       fail-fast: false
#       matrix:
#         language: [go, javascript-typescript]
#     steps:
#       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
#         with:
#           persist-credentials: false
#       - uses: github/codeql-action/init@db488ddef3bf6cb639b32c2e9a7c0a7ea8271d28 # v4.37.8
#         with:
#           languages: ${{ matrix.language }}
#       - uses: github/codeql-action/autobuild@db488ddef3bf6cb639b32c2e9a7c0a7ea8271d28 # v4.37.8
#       - uses: github/codeql-action/analyze@db488ddef3bf6cb639b32c2e9a7c0a7ea8271d28 # v4.37.8
#         with:
#           category: "/language:${{ matrix.language }}"
#